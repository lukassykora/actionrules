.. highlight:: shell

============
Installation
============


Stable release
--------------

To install Action Rules, run this command in your terminal:

.. code-block:: console

    $ pip install action_rules

This is the preferred method to install Action Rules, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for Action Rules can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/lukassykora/action_rules

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/lukassykora/action_rules/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/lukassykora/action_rules
.. _tarball: https://github.com/lukassykora/action_rules/tarball/master
